FROM node:20-alpine
{% if db == 'sqlite' %}
RUN apk add --no-cache python3 make g++
{% endif %}
WORKDIR /api
COPY package.json .
RUN npm install --production
COPY . .
EXPOSE 8000
CMD ["node", "index.js"]
